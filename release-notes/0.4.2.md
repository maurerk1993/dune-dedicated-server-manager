# 0.4.2

## Release Reliability

- Rebuilt the 0.4.1 Admin safety fixes as a fresh patch release.
- The release workflow now retries frontend dependency installation so a temporary npm network reset is less likely to block Windows installer creation.

## Included Admin Fixes

- Custom-tier item grants respect item stackability and avoid invalid multi-count stacks for non-stackable gear.
- Specialization level edits now cast Dune specialization tracks safely for Postgres.

## Notes

- No manual SQL migration is required.
- No action is needed beyond updating the app and bundled server service.
