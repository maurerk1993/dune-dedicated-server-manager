# 0.5.1

- Fixed the repo-ready item catalog export button so the save dialog is allowed by the desktop app permissions.
- Cleaned up the item catalog source display so long GitHub asset URLs no longer spill into the Recent publishes column.

No manual database action is required. After updating, refresh the Dune server in the app and apply the bundled server service update.
